#pragma once

/**
 * @brief relational algebra expression models for SQL IR.
 * This namespace directly contains operators only which appear in both intermediate and step execution plan.
 */
namespace takatori::relation {}
